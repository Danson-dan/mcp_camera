[project]
name = "mcp-camera"
version = "0.1.0"
description = "MCP Camera Tool - 让AI具备视觉能力"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "mcp[cli]>=1.23.3",
    "opencv-python>=4.10.0",
    "numpy>=1.26.0",
    "pillow>=10.0.0",
]

[project.scripts]
mcp-camera = "main:main"
